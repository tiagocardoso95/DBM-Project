<html>
    <body>
        <form method="{{method}}" action="/api/{{schemaName}}/{{id}}">
        {{#properties}}
            <div>
                    
                <label {{hidden}}>{{name}}:</label>            
                <input type="{{typeInput}}" {{hidden}} name="{{name}}" value="{{value}}"/>
                <br>
                 
            </div>        
        {{/properties}}
        <input type="submit" value="Submit"/> 
        </form>
    </body>
</html>